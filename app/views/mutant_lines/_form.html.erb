<%= form_for(@mutant_line) do |f| %>
  <% if @mutant_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mutant_line.errors.count, "error") %> prohibited this mutant_line from being saved:</h2>

      <ul>
      <% @mutant_line.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
